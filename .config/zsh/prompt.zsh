# L-Prompt
#   VCS Info -> Branch, Dirty state, Changes to receive, changes to commit
#   Change color of prompt symbol (like $) based on exit statys
#   Current Directory (perhaps trimmed down to show only 2 dirs)
# R-Prompt
#   Time in 12 Hour Format
#   Vi Mode - A small powerline like block on the left indicating I, N, Ex, or V mode
